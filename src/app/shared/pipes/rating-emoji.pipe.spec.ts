import { RatingEmojiPipe } from './rating-emoji.pipe';

describe('RatingEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
