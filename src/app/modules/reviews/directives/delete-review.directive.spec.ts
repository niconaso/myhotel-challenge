import { DeleteReviewDirective } from './delete-review.directive';

describe('DeleteReviewDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteReviewDirective();
    expect(directive).toBeTruthy();
  });
});
