import { AddReviewDirective } from './add-review.directive';

describe('AddReviewDirective', () => {
  it('should create an instance', () => {
    const directive = new AddReviewDirective();
    expect(directive).toBeTruthy();
  });
});
