import { CreateOrEditReviewDirective } from './create-or-edit-review.directive';

describe('CreateOrEditReviewDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateOrEditReviewDirective();
    expect(directive).toBeTruthy();
  });
});
